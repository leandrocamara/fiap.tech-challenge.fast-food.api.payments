apiVersion: v1
kind: Pod
metadata:
  name: fast-food-payments-database
  labels:
    app: fast-food-payments-database
spec:
  volumes:
    - name: pgdata-storage
      persistentVolumeClaim:
        claimName: pgdata-pv-volume2
  containers:
    - name: fast-food-payments-database-container
      image: postgres:latest
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
      ports:
        - containerPort: 5432
      volumeMounts:
        - mountPath: "/var/lib/postgresql/data"
          name: pgdata-storage 
      envFrom:
        - secretRef:
            name: fast-food-payments-database-secret
